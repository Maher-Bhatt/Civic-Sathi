fastapi==0.115.12
uvicorn[standard]>=0.30,<1
sqlalchemy>=2.0,<3
alembic>=1.13,<2
pydantic[email]>=2.8,<3
pydantic-settings>=2.4,<3
# BUG-014: Use only psycopg3 (psycopg[binary]) — psycopg2-binary removed to avoid
# conflicting driver resolution on Render and asyncio incompatibility.
psycopg[binary]>=3.2,<4
email-validator>=2.1.0
python-dotenv>=1.0,<2
pandas>=2.2,<3
numpy>=1.26,<3
scikit-learn>=1.5,<2
pyjwt>=2.8,<3
bcrypt>=4.0,<5
pytest>=8,<9
testcontainers[postgres]>=4.9,<5
httpx>=0.27,<1
# BUG-015: spacy is optional — only include if NLP features are explicitly needed.
# The application catches ImportError for spacy so it won't crash without it.
spacy>=3.7,<4
